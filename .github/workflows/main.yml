name: Gold and Forex Signal Alert V4 — DISABLED

# Legacy V4 runner retired. Positive Arena Markets now uses the Supabase V6 pipeline.
# Kept only as a visible record; this job can never execute.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-v4-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Legacy V4 disabled
        run: echo "Legacy V4 delivery is disabled. Use Positive Arena Markets V6 only."
